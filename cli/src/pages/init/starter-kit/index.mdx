---
navGroup: init
navOrder: 0
navLabel: select-db
---

import Terminal from '../../../components/Terminal/Terminal'
import {
  selectInputItemComponent,
  selectInputIndicatorComponent,
  Divider,
} from '../../../components/inkHelpers'
import { Box, Text, Color } from 'ink'
import SelectInput from 'ink-select-input'
import figures from 'figures'
import BorderBox from '../../../components/Terminal/BorderBox'
import chalk from 'chalk'

# `prisma2 init`

## Language selection

<Terminal>
  <Box flexDirection="column" marginLeft={2}>
    <Text bold>Select the language for your starter kit.</Text>
    <Color dim>Starter kits are available in several programming languages.</Color>
  </Box>
  <BorderBox
    flexDirection="column"
    marginTop={1}
    marginBottom={1}
    title={chalk.bold('Supported languages')}
  >
    <Box>
      <Color cyan>
        {figures.pointer} <Text bold>JavaScript</Text>
      </Color>
    </Box>
    <Box>
      <Box marginLeft={2}>TypeScript</Box>
    </Box>
    <Box>
      <Box marginLeft={2}>
        <Color dim>Go (Coming soon)</Color>
      </Box>
    </Box>
  </BorderBox>
  <Box>
    <Color gray>❮ </Color>
    <Text bold>Back</Text>
    <Color dim> (Project options)</Color>
  </Box>
</Terminal>

- [Language](#starter-kit-selection)

## Starter kit selection

<Terminal>
  <Box flexDirection="column" marginLeft={2}>
    <Text bold>Select the starter kit.</Text>
    <Color dim>You can connect the starter kit to your own database.</Color>
  </Box>
  <BorderBox
    flexDirection="column"
    marginTop={1}
    marginBottom={1}
    title={chalk.bold('Starter kits (JavaScript)')}
  >
    <Box width={100}>
      <Box>
        <Color cyan>
          <Box width={19}>
            {figures.pointer} <Text bold>GraphQL API</Text>
          </Box>
          <Color dim> GraphQL server (graphql-yoga)</Color>
        </Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={19}>
          <Font>GraphQL (+ Auth)</Font>
        </Box>
        <Color dim>GraphQL server + authentication</Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={19}>
          <Font>GraphQL (Apollo)</Font>
        </Box>
        <Color dim>GraphQL server (apollo-server)</Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={19}>
          <Font>REST API</Font>
        </Box>
        <Color dim>REST API server (Express.js)</Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={19}>
          <Font>gRPC API</Font>
        </Box>
        <Color dim>gRPC API server + client</Color>
      </Box>
    </Box>
  </BorderBox>
  <Box>
    <Color gray>❮ </Color>
    <Text bold>Back</Text>
    <Color dim> (Language selection)</Color>
  </Box>
</Terminal>

- [Starter kit](#database-selection)

> **Note**: The available examples are loaded dynamically from the [examples API](../examples-api).

> **Note**: The language `JavaScript` in `Starter kits (JavaScript)` is dynamic based on the previous selection.

## Database selection

<Terminal>
  <Box flexDirection="column" marginLeft={2}>
    <Text bold>Select the database you want to use.</Text>
    <Color dim>Use SQLite if you don't have a database running yet.</Color>
  </Box>
  <BorderBox
    flexDirection="column"
    marginTop={1}
    marginBottom={1}
    title={chalk.bold('Supported databases')}
  >
    <Box width={100}>
      <Box>
        <Color cyan>
          <Box width={15}>
            {figures.pointer} <Text bold>SQLite</Text>
          </Box>
          <Color dim> Easiest to set up</Color>
        </Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={15}>
          <Font>MySQL</Font>
        </Box>
        <Color dim>Requires running a MySQL database</Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={15}>
          <Font>PostgreSQL</Font>
        </Box>
        <Color dim>Requires running a PostgreSQL database</Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Color dim>
          <Box width={15}>MongoDB</Box>
          <Font>(Coming soon)</Font>
        </Color>
      </Box>
    </Box>
  </BorderBox>
  <Box>
    <Color gray>❮ </Color>
    <Text bold>Back</Text>
    <Color dim> (Project options)</Color>
  </Box>
</Terminal>

- [SQLite](./sqlite)
- [MySQL](./mysql)
- [PostgreSQL](./postgresql)
