---
navGroup: init
navOrder: 0
navLabel: Database selection
---

import Terminal from '../../components/Terminal/Terminal'
import {
  selectInputItemComponent,
  selectInputIndicatorComponent,
  Divider,
} from '../../components/inkHelpers'
import { Box, Text, Color } from 'ink'
import SelectInput from 'ink-select-input'
import figures from 'figures'
import BorderBox from '../../components/Terminal/BorderBox'
import chalk from 'chalk'

# `prisma2 init`

---

## Flows

<img
  src="https://prisma-specs.prisma.now.sh/cli/figma-proxy?id=822:4"
  width="100%"
/>

## Database selection

<Terminal>
  <Box marginBottom={1}><Color dim>~/Desktop $</Color> prisma init hello-world</Box>

  <Box flexDirection="column" marginLeft={2}>
    <Text bold>Select the database you want to use</Text>
    <Color dim>
      Use SQLite if you don't have a database running yet
    </Color>
  </Box>

  <BorderBox flexDirection="column" marginTop={1} marginBottom={1} title={chalk.bold('Databases')}>
    <Box width={100}>
      <Box>
        <Color cyan>
          <Box width={15}>{figures.pointer} <Text bold>SQLite</Text></Box>
          <Color dim>  Easiest to set up</Color>
        </Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={15}><Font>MySQL</Font></Box>
        <Color dim>Requires running a MySQL database</Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Box width={15}><Font>PostgreSQL</Font></Box>
        <Color dim>Requires running a PostgreSQL database</Color>
      </Box>
    </Box>
    <Box width={100}>
      <Box marginLeft={2}>
        <Color  dim>
          <Box width={15}>MongoDB</Box>
          <Font>(Coming soon)</Font>
        </Color>
      </Box>
    </Box>
  </BorderBox>
</Terminal>

- [SQLite](./sqlite)
- [MySQL](./mysql)
- [PostgreSQL](./postgresql)