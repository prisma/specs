---
navGroup: dev
navOrder: 2
navLabel: Dashboard
---

import Terminal from '../components/Terminal/Terminal'
import BorderBox from '../components/Terminal/BorderBox'
import {
  selectInputItemComponent,
  selectInputIndicatorComponent,
  Divider,
} from '../components/inkHelpers'
import SelectInput from 'ink-select-input'
import { Box, Text, Color } from 'ink'
import figures from 'figures'
import chalk from 'chalk'

# BorderBox test

---

### (4.1) Example selection

Previous selections:

1. SQLite
2. Use existing SQLite file
3. _entered file path_
4. _introspected DB_
5. Photon
6. JavaScript/TypeScript

<Terminal>
  <Box flexDirection="column" marginLeft={2} marginBottom={1}>
    <Text bold>Select an example or start from scratch</Text>
    <Color dim>Example is generated based on your database schema</Color>
  </Box>
  <BorderBox
    flexDirection="column"
    title={`${chalk.bold('Examples to get you started')}`}
    marginBottom={1}
  >
    <Box width={100}>
      <Color cyan>
        <Box width={20}>
          {figures.pointer} <Text bold>Script</Text>
        </Box>
        <Color dim>  Simple script with API examples</Color>
      </Color>
      <Box flexGrow={1}></Box>
    </Box>
    <Box width={100}>
      <Box width={20} marginLeft={2}>
        No boilerplate
      </Box>
      <Color dim>Just generate the Prisma schema</Color>
      <Box flexGrow={1} />
    </Box>
  </BorderBox>
  <Box>
    <Color gray>❮ </Color>
    <Text bold>Back</Text>
    <Color dim> (Language selection)</Color>
  </Box>
</Terminal>

**TODO**: There needs to be a separate box inside the border box, like this:

<Terminal>
  <Box flexDirection="column" paddingTop={2} paddingBottom={1}>
    <Box>
      <Color gray>┌──</Color>
      <Text bold> Boilerplates </Text>
      <Color gray>────────────────────────────────────────────┐</Color>
    </Box>
    <Box width={100}>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Color cyan>
        <Box width={20}>
          {figures.pointer} <Text bold>From scratch</Text>
        </Box>
        <Color dim> Simple script with API examples</Color>
      </Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Box width={20} marginLeft={2}>
        GraphQL boilerplate
      </Box>
      <Color dim>GraphQL server example</Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Box width={20} marginLeft={2}>
        REST boilerplate
      </Box>
      <Color dim>REST API example (using express.js)</Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Box width={20} marginLeft={2}>
        gRPC boilerplate
      </Box>
      <Color dim>gRPC API example (client + server)</Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>
        ├────────────────────────────────────────────────────────────┤
      </Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Box width={20} marginLeft={2}>
        No boilerplate
      </Box>
      <Color dim>Just generate the Prisma schema</Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>│ </Color>
      <Box flexGrow={1} />
      <Color gray> │</Color>
    </Box>
    <Box width={100}>
      <Color gray>
        └────────────────────────────────────────────────────────────┘
      </Color>
    </Box>
  </Box>
</Terminal> 

<!-- 
## You can use `extension={true}`

<Terminal>
  <BorderBox
    flexDirection="column"
    height={4}
    title={`${chalk.bold('Hi')} ${chalk.dim.yellow('- this is yellow')}`}
    justifyContent="space-between"
  >
    <Box>This is a box</Box>
    <Box>This is another box</Box>
  </BorderBox>
  <BorderBox
    flexDirection="column"
    height={4}
    title={`${chalk.bold('Hi')} ${chalk.dim.yellow('- this is yellow')}`}
    justifyContent="space-between"
    extension={true}
  >
    <Box>This is a box</Box>
    <Box>This is another box</Box>
  </BorderBox>
  <BorderBox
    flexDirection="column"
    height={4}
    title={`${chalk.bold('Hi')} ${chalk.dim.yellow('- this is yellow')}`}
    justifyContent="space-between"
    extension={true}
  >
    <Box>This is a box</Box>
    <Box>This is another box</Box>
  </BorderBox>
  <BorderBox
    flexDirection="column"
    height={4}
    title={`${chalk.bold('Hi')} ${chalk.dim.yellow('- this is yellow')}`}
    justifyContent="space-between"
    marginTop={1}
  >
    <Box>This is a box</Box>
    <Box>This is another box</Box>
  </BorderBox>
</Terminal> -->
