import TerminalApp from '../../components/TerminalApp'
import {
  selectInputItemComponent,
  selectInputIndicatorComponent,
  Divider,
} from '../../components/inkHelpers'
import SelectInput from 'ink-select-input'
import { Box, Text, Color } from 'ink'

## Flows

<img
  src="https://prisma-specs.prisma.now.sh/cli/figma-proxy?id=822:4"
  width="100%"
/>

## Selecting a Database

export const tools = [
  { label: 'Photon', value: 'photon' },
  { label: 'Lift', value: 'lift' },
]

<TerminalApp>
  <Box flexDirection="column">
    <Text bold>Select the database you want to use</Text>
    <Divider />
  </Box>
  <Box marginBottom={10}>
    <Box width={20} flexDirection="column">
      <SelectInput
        items={[
          { label: 'SQLite', value: 'sqlite' },
          { label: 'MySQL', value: 'mysql' },
          { label: 'PostgreSQL', value: 'postgresql' },
        ]}
        itemComponent={selectInputItemComponent}
        indicatorComponent={selectInputIndicatorComponent}
      />
      <Box marginLeft={2}>
        <Color gray>MongoDB</Color>
      </Box>
    </Box>
    <Box flexDirection="column">
      <Color gray>Easies to set up</Color>
      <Color gray>Requires running a MySQL database</Color>
      <Color gray>Requires running a PostgreSQL database</Color>
      <Color gray>(Coming soon)</Color>
    </Box>
  </Box>
</TerminalApp>

## 1 Database options

Previous selections:

1. SQLite

<TerminalApp>
  <Box flexDirection="column">
    <Text bold>How do you want to proceed?</Text>
    <Divider />
  </Box>
  <Box marginBottom={10}>
    <Box width={40}>
      <SelectInput
        items={[
          { label: 'Create new SQLite file', value: 'new-sqlite' },
          { label: 'Use existing SQLite file', value: 'existing-sqlite' },
        ]}
        itemComponent={selectInputItemComponent}
        indicatorComponent={selectInputIndicatorComponent}
      />
    </Box>
    <Box flexDirection="column">
      <Color gray>Easy setup to start from scratch</Color>
      <Color gray>Requires the path to the file</Color>
    </Box>
  </Box>
</TerminalApp>
